// App.js
import { registerRootComponent } from 'expo';
import App from './index';         // or './src/App' if your main component lives there

// This runs your Expo app:
registerRootComponent(App);
